transition to-do:
	new tables definiitons
		//write function to convert old style to new style
		//make mapped objs a memoized function		
		//auto calculate WCAG attributes
		//add unique id to each td element for horizontal comparison
    add backbone routes
		investigate using d3 to create the tables declaratively
    add invidivual dispatch call listeners (example, add qfr links for qfr tables)
	data
		add universal data acess functions across the tables definitions
	d3
		create tooltip templates
	create secondary landing page for choice between graphics heavy and text based
	convert long text over to markdown
	remove jqnumberformat in favour of smaller library
	jshint all the files
	concat all the files together
	add more documentation for docco	
