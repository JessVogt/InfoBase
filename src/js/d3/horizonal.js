(function() {
    var D3 = ns('D3'),
    TOOLTIP = D3.TOOLTIP,
    PACK = ns('D3.PACK'),
    TABLES = ns('TABLES');

    D3.horizontal_gov =  function(app){
      return new _horizontal_goc(app);
    };

    _bubbleDeptList = function(app){
      var lang = app.state.get("lang"),

      app.app.hide();






    };
})();
